

## lued expects scripts and plugins to be in ~/.lued
## create a sym link from lua_src to ~/.lued
echo "ln -sf $PWD/lua_src  ~/.lued"
ln -sf $PWD/lua_src ~/.lued
echo " "
echo "Try it with: ./lued README.md"
echo "Add to path: cp lued ~/bin or any directory in path"

